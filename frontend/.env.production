# When deploying to Vercel (frontend-only), leave VITE_API_URL blank.
# The app will run MediaPipe Pose entirely in the browser.
# Only set this if you have a deployed backend instance.
VITE_API_URL=
